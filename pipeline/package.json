{
  "name": "nordstemmen-pipeline",
  "version": "1.0.0",
  "description": "Document processing pipeline: PDF → OCR → Embeddings → Qdrant → B2",
  "type": "module",
  "scripts": {
    "pipeline": "node --experimental-strip-types src/index.ts",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@google/genai": "^0.14.0",
    "@qdrant/js-client-rest": "^1.12.0",
    "dotenv": "^17.3.1",
    "uuid": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.3.0",
    "vitest": "^3.0.0"
  }
}
